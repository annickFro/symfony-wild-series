{% extends 'base.html.twig' %}

{% block title %}Wild Series -dans category/show-{% endblock %}

{% block body %}
<h1>Form Category </h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Add category</button>
    {{ form_end(form) }}
{% endblock %}
