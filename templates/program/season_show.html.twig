{% extends 'base.html.twig' %}

{% block title %}Wild Series{% endblock %}

{% block body %}
<section class="container">
    <h1>Saison {{ seasonId }} de <em>{{ program.title }}</em></h1>
    <h2>{{ program.synopsis }}</h2>
    <h3>Liste des épisodes</h3>
        <ul>
            {% for episode in episodes %}
                <li>Episode {{ episode.id }} {{ episode.number }} : {{ episode.title }} </li>
                {# <a href="{{episode.id}}">Lien</a> #}
<a href="{{ path("program_episode_show", { programId: program.id, seasonId: season.id, episodeId: episode.id }) }}">Lien vers les épisodes</a>

            {% endfor %}
        {# {{ dump(programId, episodes, program) }}
        {{ dump(program.id) }}
        {{ dump(seasonId) }}
        {{ dump(episode.id) }} #}
        </ul>
    <pre>
    </pre>
</section>
{% endblock %}
